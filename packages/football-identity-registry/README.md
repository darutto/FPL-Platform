# football-identity-registry

Future provider-neutral wrapper around the repository's existing identity
mechanisms.

FI-0(b) provides package scaffolding only. Existing player registries and
matching behavior are unchanged.
