# Weekly-workflow (build-time) dependencies only — the serving deployment
# reads the parquet store from R2 and must NOT install soccerdata.
soccerdata>=1.9.0
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0  # fpl_tactical.paths imports fpl_data_core.season_registry (single source of truth for CURRENT_SEASON)
